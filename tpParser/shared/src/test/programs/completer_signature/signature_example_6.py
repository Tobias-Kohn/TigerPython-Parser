# 181
# attrs : dict[str, int], grid : list[list[int]], tuples : tuple[tuple[str, int], tuple[int, list[str]]]
class Foo:
    def __init__(self):
        pass
    def bar(attrs: dict[str, int], grid: list[list[int]], tuples: tuple[tuple[str, int], tuple[int, list[str]]]):
        pass
Foo().bar()
